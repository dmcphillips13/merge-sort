const sort = (arr) => {
    if(arr.length <= 1) {
        return arr;
    }
    const midpoint = arr.length / 2;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left,]
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...]
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, .]
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift())
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            re
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {

        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift())
        } else {

        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left
        } else {

        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left)
        } else {
            
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left)
        } else 
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left)
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] > right[0]) {
            results.push(left)
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] > right[0]) {
            results.push()
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] )
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length )

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    return arr;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    cost right = sort(arr.slice(midpoint));
    return arr;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    cost right = sort(arr.slice(midpoint);
    return arr;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    cost right = arr.slice(midpoint);
    return arr;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint);
    cost right = arr.slice(midpoint);
    return arr;
};

module.exports = sort;
    const left = arr.slice(0, midpoint);
    cost right = arr.slice(midpoint);
    return arr;
};

module.exports = sort;
    const left = 
    return arr;
};

module.exports = sort;
    const le
    return arr;
};

module.exports = sort;

    return arr;
};

module.exports = sort;
    return arr;
};

module.exports = sort;
    return arr;
};

module.exports = sort;
};

module.exports = sort;
        return arr;
    }lengthmodule()arr <= 1) {
      .exports = sort;  return arr;
    }lengthmodule()arr <= 1) {
      .exports = sort;  return arr;
    }lengthmodule    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left, ...right];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...left,]
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, ...]
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    results = [...results, .]
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift());
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            results.push(right.shift())
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {
            re
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift());
        } else {

        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left.shift())
        } else {

        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left
        } else {

        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left)
        } else {
            
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left)
        } else 
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] < right[0]) {
            results.push(left)
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] > right[0]) {
            results.push(left)
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] > right[0]) {
            results.push()
        }
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        if(left[0] )
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length && right.length) {
        
    }

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    while(left.length )

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    

    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    return results;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    let results = [];
    
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
    
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    const right = sort(arr.slice(midpoint));
    return arr;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    cost right = sort(arr.slice(midpoint));
    return arr;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    cost right = sort(arr.slice(midpoint);
    return arr;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint));
    cost right = arr.slice(midpoint);
    return arr;
};

module.exports = sort;
    const left = sort(arr.slice(0, midpoint);
    cost right = arr.slice(midpoint);
    return arr;
};

module.exports = sort;
    const left = arr.slice(0, midpoint);
    cost right = arr.slice(midpoint);
    return arr;
};

module.exports = sort;
    const left = 
    return arr;
};

module.exports = sort;
    const le
    return arr;
};

module.exports = sort;

    return arr;
};

module.exports = sort;
    return arr;
};

module.exports = sort;
    return arr;
};

module.exports = sort;
};

module.exports = sort;
        return arr;
    }lengthmodule()arr <= 1) {
      .exports = sort;  return arr;
    }lengthmodule()arr <= 1) {
      .exports = sort;  return arr;
    }lengthmodule